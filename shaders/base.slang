#ifndef ENGINE_BASE_SLANG
#define ENGINE_BASE_SLANG

// raster compile units: passes/mesh_raster.slang + passes/mesh_fragment.slang
#include "types.slang"
#include "core/binding.slang"
#include "geometry/mesh.slang"
#include "material/pbr.slang"

#endif
