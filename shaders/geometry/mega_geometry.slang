#ifndef ENGINE_GEOMETRY_MEGA_GEOMETRY_SLANG
#define ENGINE_GEOMETRY_MEGA_GEOMETRY_SLANG

#include "types.slang"

struct ClusterDraw
{
    uint clusterId;
    uint lod;
    uint instanceId;
    uint64_t clasAddress;
};

#endif
